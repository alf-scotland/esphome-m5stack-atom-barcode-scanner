# Deprecated: the primary firmware file is now atom_lite.yaml.
# This stub exists for backwards compatibility with existing OTA installations
# and CI tooling that referenced firmware.yaml.  It will be removed once all
# devices have been migrated to update from atom_lite.yaml.
packages:
  device: !include atom_lite.yaml
